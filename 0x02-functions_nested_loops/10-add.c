#include "main.h"
#include <stdio.h>
/**
* add - add two numbers
* @n: the first number
* @n: the second number
*Return: The addition of both numbers
*/
int add(int a, int b)
{
int result = a + b;
return (result);
}
